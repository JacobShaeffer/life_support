<%= bootstrap_form_with(model: zone) do |form| %>
  <% if zone.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(zone.errors.count, "error") %> prohibited this zone from being saved:</h2>

      <ul>
        <% zone.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.text_field :name %>
  </div>

<!--  <div>-->
		<%#= form.label "Parent"%>
    <%#= form.collection_select :parent_id, Zone.all, :id, :name, {:include_blank => "None", skip_label: true} %>
<!--  </div>-->

  <div>
    <%= form.submit %>
  </div>
<% end %>
